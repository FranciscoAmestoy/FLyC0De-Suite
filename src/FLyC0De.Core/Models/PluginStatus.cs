namespace FLyC0De.Core.Models
{
    public enum PluginStatus
    {
        Active,     // Green LED
        Inactive,   // Red LED (Available but disabled)
        Locked      // Red LED (Requires purchase)
    }
}
